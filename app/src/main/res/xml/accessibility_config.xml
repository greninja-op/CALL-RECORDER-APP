<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility service configuration for the Unified Call Recorder.

    Scoped strictly to the Target_Package set (Requirement 1.1): the Google/AOSP/Samsung
    dialers plus WhatsApp. The service retrieves window content so the core can classify
    in-call windows (InCallActivity / InCallUI / VoipActivity). Runtime event delivery is
    device-only / manual verification.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:packageNames="com.google.android.dialer,com.android.incallui,com.samsung.android.incallui,com.whatsapp" />
